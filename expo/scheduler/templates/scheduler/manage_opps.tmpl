{% comment %}

included in event edit flow. If there is an actionform, show it

{% endcomment %}



<div class=bid_col><div class=side_box>
  <h2>Volunteer Management </h2>
  <table>
    <tr> 
    {% for header in actionheaders %}
      <th> {{ header }} 
    {% endfor %}
    </tr>
    {% for form in actionform %}
    <tr>
      <form method="POST" action="{% url 'scheduler:manage_opps'  event_id %}" 
      	    enctype="multipart/form-data">
        {% csrf_token %}
	{% for field in form %}
 	  <td> {{field}} </td>
 	{% endfor %}	    	
	{% for field in form.hidden_fields %}
	  {{field}}
	{% endfor %}
	  <td>  <input type = "submit" name="edit" value="Edit Opportunity"></td>
	  <td>  <input type = "submit" name="delete" value="Delete"></td>
	  <td>  <input type = "submit" name="allocate" value = "Allocate Workers" <td>
      </form>	      
    </tr>
    {% endfor %}
    <tr>
      <form method="POST" action="{% url 'scheduler:manage_opps' event_id  %}" 
      	    enctype="multipart/form-data">
        {% csrf_token %}
	{% for field in createform %}
	  <td> {{field}} </td>
        {% endfor %}	    		      
 	<td>  <input type = "submit" name="create" value="Create Opportunity"></td>
      </form>	      
    </tr>
  </table>
  <br><br>
</div></div>

